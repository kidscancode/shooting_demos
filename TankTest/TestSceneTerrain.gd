extends Spatial

#func _ready():
#	$MeshInstance.mesh.material.next_pass.set_shader_param("radius", 0.05)
#
#func _process(delta):
#	var pos = $MiniTank.global_transform.origin
#	var spos = Vector2(pos.x, pos.z)	 / $MeshInstance.mesh.size + Vector2(0.5, 0.5);
##		print(pos)
#	$MeshInstance.mesh.material.next_pass.set_shader_param("pos", spos)
